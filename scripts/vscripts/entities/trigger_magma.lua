--- IsServer -> true
---@param params {outputid:number, caller:handle, activator:handle}
function OnStartTouch(params)
	-- local hActivator = params.activator
	-- if IsValid(hActivator) then
	-- 	hActivator.bTouchMagma = true
	-- 	if hActivator.IsWave and hActivator:IsWave() then
	-- 		hActivator:AddNewModifier(nil, nil, "modifier_enemy_touch_magma", { duration = 0.3 })
	-- 	end
	-- end
end

function OnEndTouch(params)
	-- local hActivator = params.activator
	-- if IsValid(hActivator) then
	-- 	hActivator.bTouchMagma = false
	-- end
end